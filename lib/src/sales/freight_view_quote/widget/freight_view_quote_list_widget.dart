import 'package:textfield_tags/textfield_tags.dart';

import '../freight_view_quote.dart';

class FreightViewQuoteListWidget extends StatefulWidget {
  const FreightViewQuoteListWidget({
    super.key,
  });

  @override
  State<FreightViewQuoteListWidget> createState() => _CreateState();
}

class _CreateState extends State<FreightViewQuoteListWidget> {
  final List<String> _soData = [''];
  final _stringTagController = StringTagController();

  @override
  void initState() {
    // TODO: implement initState
    super.initState();
  }

  @override
  void dispose() {
    // TODO: implement dispose
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
